# Core Dependencies
fastapi>=0.104.0,<0.117.0
uvicorn[standard]>=0.24.0,<0.36.0
websockets>=11.0.0,<12.0.0

# LangGraph for agent workflows
langgraph>=0.0.20,<0.7.0

# Azure OpenAI and AI libraries
openai>=1.3.0,<2.0.0

# Database connectivity
neo4j>=5.13.0,<6.0.0

# Configuration management
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.0.3,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# Data handling
dataclasses-json>=0.6.0

# Standard library extensions
pathlib2>=2.3.7; python_version < "3.4"
typing-extensions>=4.8.0

# Development and testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Logging (standard library, but good to be explicit about versions for extensions)
# logging is part of standard library, no need to specify

# JSON handling (standard library)
# json is part of standard library

# Time handling (standard library) 
# time is part of standard library
